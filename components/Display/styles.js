import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 20
    },
    display: {
        fontSize: 50,
        color: "white",
        textAlign: 'right'
    }
})

export default styles